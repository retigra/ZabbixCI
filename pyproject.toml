[project]
name = "zabbix-git"
version = "0.1.0"
description = "Zabbix Git"
authors = [
    {name = "Wouter de Bruijn", email = "wouter.debruijn@retigra.nl"}
]
dependencies = [
    "zabbix_utils",
    "python-dotenv",
    "ruamel.yaml",
    "regex",
    "pygit2",
]

[tool.setuptools.packages.find]
exclude = ["tests", "cache"]

[project.scripts]
zabbix-git = "zabbix_git.cli:parse_cli"